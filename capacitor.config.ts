import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.matthewkissinger.fallingsandalchemy',
  appName: 'Falling Sand Alchemy',
  webDir: 'dist'
};

export default config;
